{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Run SSG",
      "command": "./run.sh",
      "options": {
        "cwd": "${workspaceFolder}/ssg/src"
      }
    },
    {
      "type": "shell",
      "label": "Live Server",
      "command": "live-server .",
      "options": {
        "cwd": "${workspaceFolder}/site"
      }
    }
  ]
}